# Representa las coordenadas de 3 puntos en un plano con tuplas: (x, y)
# para cada punto. Luego imprime solo las coordenadas x de todos.

coordenadas = ((3.14, 500), (369, 963), (-45, 21))
for punto in coordenadas:
    print(punto[0])
